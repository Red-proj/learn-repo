name: maxbot-go CI

on:
  push:
    paths:
      - 'Progs/libMax/maxbot-go/**'
      - '.github/workflows/maxbot-go-ci.yml'
  pull_request:
    paths:
      - 'Progs/libMax/maxbot-go/**'
      - '.github/workflows/maxbot-go-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Progs/libMax/maxbot-go
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: Progs/libMax/maxbot-go/go.mod

      - name: Test
        run: go test ./...
